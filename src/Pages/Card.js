import React from 'react'

function Card(props) {
    return (
        <div>
               {props.name}
               {props.age}
               {props.tittle}

        </div>
    )
}

export default Card
